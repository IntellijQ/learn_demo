需求说明：
    举例来说，有一个字符串"BBC ABCDAB ABCDABCDABDE"，我想知道，里面是否包含另一个字符串"ABCDABD"？

解决方案：
    暴力解决
    KMP

为什么会使用kmp
    因为在暴力解决的过程中，会重复比较已经比较的字符
    使用kmp的目的就是避免该情况的出现，
    KMP算法的想法是，设法利用这个已知信息，不要把"搜索位置"移回已经比较过的位置，继续把它向后移，这样就提高了效率。

使用kmp算法步骤
    1.创建部分匹配值表
    2.搜索匹配

要点说明：
    1.真前缀：ABCDABD的真前缀 除最后一个字符D 以外的连续字符串，如 A, AB, ABC, ABCD, ABCDA
    2.真后缀: ABCDABD的真后缀 除第一个字符A 以外的连续字符串，如 BCDAB, CDAB, DAB, AB, B
    3.部分匹配值：字符串的 "前缀"和"后缀"的最长的共有元素的长度